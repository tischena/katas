# Katas

This repository contains solutions to various coding tasks (katas) along with their corresponding tests. Each task is implemented as a Python function, and testing is done to ensure correctness and reliability.

## Structure

- `fundamentals/` - Contains Python modules with implementations of different tasks.
- `tests/` - Contains test functions for each implementation.
